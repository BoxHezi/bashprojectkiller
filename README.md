# Bash Sciprt to kill process

* Bash script that kill process by process (partial) name
* change the bash file permission to 755: `chmod 755 kill.sh`
* Run the scirpt: `./kill.sh [process name]`
* Please run as root user